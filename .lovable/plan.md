## Where "Generate" lives today

Step 1 (the screen you're on) is **preview only** — it shows what was parsed from Week 1 and a **Continue** button. There is no Generate button here.

The actual generate controls appear on **Step 4 · Build Ads**, after you walk through:

```
Step 1 Calendar  →  Step 2 Weeks  →  Step 3 Style  →  Step 4 Build Ads  →  Step 5 Launch
   (preview)       (pick weeks)     (pick style)     (Generate here)      (finish)
```

On Step 4 you'll see:
- **Generate all (N)** button at the top of the panel
- A per-post **Generate** button on each queued card
- A **Generate week** shortcut in the week header

Click **Continue** on the current screen twice (Weeks is preselected to Week 1, Style has defaults) and you'll land on Build Ads where Generate is available.

## Optional improvement

If you'd like, I can add a **"Generate Week 1 now"** shortcut directly on Step 1 so you can skip Steps 2–3 when the defaults are fine. It would:

- Auto-select Week 1, apply default style, jump straight to Step 4, and immediately kick off `Generate all` for that week's posts.
- Keep the existing Continue flow for anyone who wants to customize weeks/style first.

Say the word and I'll build it; otherwise this plan is just the locator answer above.